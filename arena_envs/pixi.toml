[workspace]
authors = ["Andras Polgar <azazdeaz@gmail.com>"]
channels = ["conda-forge"]
name = "arena_envs"
platforms = [{ platform = "linux-64", cuda = "12.0" }]
version = "0.1.0"

[tasks]
# install-isaaclab = { cmd = "cd ../submodules/IsaacLab && chmod +x isaaclab.sh && ./isaaclab.sh --install"}
install-isaaclab = { cmd = "./isaaclab.sh --install", cwd = "../submodules/IsaacLab"}

[dependencies]
python = "3.12.*"
pip = "*"


[pypi-dependencies]
isaaclab-arena = { path = "../submodules/IsaacLab-Arena", editable = true }
